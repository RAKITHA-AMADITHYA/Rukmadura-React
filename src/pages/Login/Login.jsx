import { Box, Button } from "@mui/material";

const Login = () => {
  return (
    <div>
      <h1>Login</h1>
      <Button variant="contained">Login</Button>
      <Box height={1000}></Box>
    </div>
  );
};

export default Login;
